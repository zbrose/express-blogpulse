<h1><%= article.title %> <small>by <%= article.author.firstName %></small></h1>

<p>
  <strong><%= moment(article.createdAt).calendar() %></strong>
</p>

<p><%= article.content %></p>

<% article.tags.forEach(function(t){ %>
  <a href="/tags/<%= t.id %>">
    #<%= t.content %>
  </a>
<% }); %>

<hr />

<form action="/articles/<%= article.id %>/comments" method="POST">
  <h2>Add Comment</h2>
  <div class="form-group">
    <label>Name</label>
    <input name="name" type="text" class="form-control" placeholder="Enter name or leave blank for Anonymous">
  </div>
  <div class="form-group">
    <label>Comment</label>
    <textarea name="content" class="form-control" placeholder="Enter comment"></textarea>
  </div>
  <div class="form-group">
    <input type="submit" class="btn btn-default">
  </div>
</form>

<h2>Comments</h2>

<% article.comments.forEach((comment, index) => { %>
  <div class="well">
    <h4><%= comment.name %></h4>
    <p><%= comment.content %></p>
  </div>
<% }); %>

<a href="/">&larr; Back Home</a>
